#include "tree_t.hpp"

tree_t::tree_t(std::vector<float> A, int B, int C) : ini(B),
                                                     fin(C)
{
    nodos(A);
}

tree_t::~tree_t()
{
}

void tree_t::nodos(std::vector<float> A)
{
    for (unsigned int i = 0; i < costes.size(); i++)
    {
    }
}